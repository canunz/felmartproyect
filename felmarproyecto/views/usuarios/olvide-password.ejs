<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recuperar Contraseña - Felmart</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <% if (locals.messages && messages.error) { %>
  <div class="alert-message error">
    <i class="fas fa-exclamation-circle"></i>
    <span><%= messages.error %></span>
    <span class="close-btn">&times;</span>
  </div>
  <% } %>

  <% if (locals.messages && messages.success) { %>
  <div class="alert-message success">
    <i class="fas fa-check-circle"></i>
    <span><%= messages.success %></span>
    <span class="close-btn">&times;</span>
  </div>
  <% } %>

  <div class="auth-wrapper">
    <a href="/login" class="back-arrow">
      <i class="fas fa-arrow-left"></i>
    </a>
    
    <div class="auth-container">
      <div class="info-panel">
        <div class="logo-container">
          <img src="/img/logo.png" alt="Felmart Logo" class="logo">
        </div>
        
        <h2 class="info-title">Recuperación de Contraseña</h2>
        
        <p class="info-text">
          Ingresa tu correo electrónico y te enviaremos las instrucciones
          para restablecer tu contraseña.
        </p>
        
        <div class="important-box">
          <span class="important-label">IMPORTANTE:</span>
          <p>Revisa tu bandeja de entrada y la carpeta de spam después de
          solicitar el restablecimiento de contraseña.</p>
        </div>
      </div>
      
      <div class="form-panel">
        <h1 class="form-title">Recuperar Contraseña</h1>
        
        <form action="/usuarios/olvide-password" method="POST">
          <div class="form-group">
            <label for="email" class="form-label">Correo electrónico*</label>
            <input type="email" id="email" name="email" class="form-control" required>
          </div>
          
          <button type="submit" class="login-btn">Enviar instrucciones</button>
          
          <div class="form-footer">
            <p>¿Recordaste tu contraseña? <a href="/login" class="register-link">Volver al inicio de sesión</a></p>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const closeBtns = document.querySelectorAll('.close-btn');
      closeBtns.forEach(btn => {
        btn.addEventListener('click', function() {
          this.closest('.alert-message').style.display = 'none';
        });
      });
    });
  </script>
</body>
</html> 